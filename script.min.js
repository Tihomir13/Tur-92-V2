const header=document.querySelector('.header'),currentYear=new Date().getFullYear();window.addEventListener('scroll',()=>{window.scrollY>0?header.classList.add('shadow'):header.classList.remove('shadow')},{passive:!0});function updateYear(){const el=document.querySelector('.year');el&&(el.textContent=currentYear)}updateYear();

